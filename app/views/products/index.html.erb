<p id="notice"><%= notice %></p>

<h1>Products</h1>

<table>
  <thead>
    <tr>
      <th>Company</th>
      <th>Title</th>
      <th>Size</th>
      <th>Price</th>
      <th>Imagestring</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <!--<% @products.each do |product| %>-->
    <!--  <tr>-->
    <!--    <td><%= product.company %></td>-->
    <!--    <td><%= product.title %></td>-->
    <!--    <td><%= product.size %></td>-->
    <!--    <td><%= product.price %></td>-->
    <!--    <div class='col-md-3'>-->
    <!--    <td><%= image_tag(product.imageString, class: "img-responsive") %></td>-->
    <!--    </div>-->
    <!--    <td><%= link_to 'Show', product %></td>-->
    <!--    <td><%= link_to 'Edit', edit_product_path(product) %></td>-->
    <!--    <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
    <!--  </tr>-->
    <!--<% end %>-->
    
    <!--<div class="container-fluid">-->
    <% @products.in_groups_of(4, false).each do |group| %>
    <div class='row'>
      <% group.each do |product| %>
        <div class='col-6 col-3'>
          <%= image_tag(product.imageString, class: "img-fluid") %>
          <h3><%= product.title %></h3>
          <%= session[:current_user] %>
 
          <%= link_to 'Edit', edit_product_path(product) %>
          <div><%= number_to_currency(product.price) %></div> 
          
        </div>
      <% end %>
    </div>
  <% end %>
  </tbody>
</table>

<br>
<!--</div>-->

<%= link_to 'New Product', new_product_path %>
