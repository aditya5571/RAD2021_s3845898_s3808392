class SavedsController < ApplicationController
  def update
    # @items = Item.all

    # saved = Saved.where(item: Item.find(params[:item]), user: current_user)
    # if saved == []
    #   Saved.create(item: Item.find(params[:item]), user: current_user)
    #   @saved_exists = true
    # else
    #   saved.destroy_all
    #   @saved_exists = false
    # end

    # respond_to do |format|
    #   format.html { }
    #   format.js { }
    # end
  end

end
